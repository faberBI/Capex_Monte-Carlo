# requirements.txt — app DCF Monte Carlo (Streamlit)
# = la tua lista attuale + le DUE righe mancanti (scipy, python-docx), segnalate con <-- NUOVO

streamlit>=1.25.0
numpy>=1.24.0
scipy>=1.10.0          # <-- NUOVO: motore (distribuzioni, copula, ndtri/betaincinv, rankdata)
pandas>=2.1.0
matplotlib>=3.7.0
plotly
numpy_financial
openpyxl               # lettura degli Excel caricati
XlsxWriter>=3.1.0      # export Excel
python-docx>=1.1.0     # <-- NUOVO: generazione del report Word (import: docx)
openai>=1.0.0          # sintesi AI del report
xlrd                   # solo per damodaran_update.py (lettura .xls); non serve all'app

# --- extra della tua app (lasciali) ---
fpdf2>=2.7.4
kaleido>=0.2.1
oauth2client>=4.1.3
Pillow

# --- opzionale: sintesi AI con Claude invece di OpenAI ---
# anthropic>=0.39.0
